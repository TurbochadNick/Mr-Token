# Ownership Gate: mr_token

Question: Who owns the IP? Zach (Python backend) and Nick (TS/web) co-built it, and it is an active BYU TTO pilot, so BYU may also assert a claim.

Involved: Nick (TurbochadNick, jncc2@byu.edu); BYU TTO.

Remote: `git@github.com:TurbochadNick/Mr-Token.git` (hosted under Nick's account, not the BYUTTO org).

Closes when: BYU TTO confirms it has no ownership claim, AND Zach and Nick formalize the ownership split in writing.

Then: move out of `gate-pending/` to `personal/` (or a shared/business bucket) per the outcome.
