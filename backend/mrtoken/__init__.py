"""MR Token transcript backend."""

# Runtime version source. Read at runtime (so `git pull` updates it in an
# editable install without a reinstall). Keep in sync with pyproject.toml's
# version; bump on every tester-facing release so `--version` and the export's
# tool_version stay meaningful. See docs/UPDATING.md.
__version__ = "0.3.0"
