The test suite `test_hugelib.py` is failing. Fix the bugs in the `mod00.py` …
`mod15.py` modules until `python3 -m pytest -q` passes.

Each module has several small functions with buggy bodies. The correct behavior
for every function is stated in that module's **BEHAVIOR REFERENCE** docstring at
the top of the file — read it before fixing. Do **not** edit `test_hugelib.py`
(it defines the expected behavior).

Work through the modules until the whole suite is green.
