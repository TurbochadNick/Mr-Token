# Task: fix the failing tests in stringkit

The `stringkit.py` module has several bugs that make its test suite fail. Your
job is to fix `stringkit.py` so that the full test suite passes.

Rules:
- Run the tests with `pytest -q`.
- Fix the bugs in `stringkit.py` only.
- Do NOT edit `test_stringkit.py` — the tests define the required behavior.
- You are done when `pytest -q` reports all tests passing.

Work until the suite is green.
